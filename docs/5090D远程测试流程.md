# 5090D远程测试流程

本文记录 BabelEcho 在 5090D 上做真实 DeepSeek + 本地 TTS 验证的固定流程。目标是让任意新 session 进入项目后，都能按同一套步骤复现实验，而不是凭聊天记录回忆命令。

## 适用范围

- 本机 MacBook 负责代码修改、测试、提交和 push。
- 5090D 负责真实运行：DeepSeek adapt、CosyVoice TTS、assemble、publish。
- 当前只验证用户提供的单个 URL、单个 source config，或 audio-first 短音频 smoke；不做订阅扫描、多集批处理或后台服务。
- 真实 runtime config、DeepSeek key、TTS wrapper 配置和生成音频均保持 ignored，不提交。

## 固定环境

- SSH alias：`my-5090d-host`
- 5090D 仓库路径：`/home/th5090d/Develop/personal_project/BabelEcho`
- 5090D Python：`.conda/babelecho-dev/bin/python`
- 5090D TTS wrapper：`/home/th5090d/miniforge3/envs/babelecho-tts/bin/tts-wrapper`
- 5090D OpenAI Whisper Python：`/home/th5090d/miniforge3/envs/babelecho-tts/bin/python`
- 5090D OpenAI Whisper CLI：`/home/th5090d/miniforge3/envs/babelecho-tts/bin/whisper`
- 5090D diarization Python：`/home/th5090d/miniforge3/envs/babelecho-diarization/bin/python`
- 真实 full-chain config：`workspace/config/local-cosyvoice-on-demand-chunked.yaml`

注意：上面的 config 是 ignored runtime config。文档只记录文件名和用途，不记录密钥或文件内容。

## 本地提交和推送

在需要 5090D 跑真实验证前，先在本机完成本地验证并推送：

```bash
git status --short --branch
.conda/babelecho-dev/bin/python -m pytest -v
git diff --check
git add <changed-files>
git commit -m "<message>"
git push
```

如果用户明确说本 session 不需要每次提交前做隐私扫描，可以跳过扫描；否则提交前按 `AGENTS.md` 的 secret / privacy scanning 规则执行。

## 5090D同步

本机 push 后，在 5090D 上只做 fast-forward pull，不在远端手改代码：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && git status --short --branch && git pull --ff-only && git status --short --branch && git --no-pager log --oneline -3'
```

如果远端出现 unrelated untracked runtime 文件，例如 `workspace/sources/*.txt` 或历史 `workspace/runs/`，不要删除，除非用户明确要求。

## DeepSeek前置质量门禁

`normalize` 阶段会写：

```text
workspace/runs/<run-id>/transcript/quality.json
```

真实 LLM provider 进入 `adapt` 前必须通过本地 deterministic 质量门禁：

- `safe_to_adapt`：允许进入 DeepSeek / local_vLLM adapt。
- `inspect_first`：阻断，不调用 DeepSeek。
- `reject`：阻断，不调用 DeepSeek。
- 缺少 `quality.json`：阻断，不调用 DeepSeek。

fixture/fake test provider 不受这个门禁影响，因为它们不消耗 DeepSeek 或真实 LLM 资源。

当前默认 DeepSeek 改写风格是 `adapt.style: faithful_spoken`。它要求尽量忠实保留原 transcript 的信息、顺序、语气、问题、数字、人名和观点组织，只做字幕噪声、舞台提示、转写说明、URL/缩写读法等轻清理，并让中文适合 TTS。需要更自然、更松的中文播客化改写时，才在 ignored runtime config 里显式设置 `adapt.style: polished_spoken`。

因此，测试新来源时优先先跑到 `normalize`，看质量报告，再决定是否继续：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && .conda/babelecho-dev/bin/python -m babelecho episode convert --workspace workspace --run-id <run-id> --url "<url>" --local-config workspace/config/local-cosyvoice-on-demand-chunked.yaml --to-stage normalize'
```

查看质量报告：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && .conda/babelecho-dev/bin/python - <<'"'"'PY'"'"'
import json
from pathlib import Path
run = Path("workspace/runs/<run-id>")
print(json.dumps(json.loads((run / "transcript/quality.json").read_text()), ensure_ascii=False, indent=2))
PY'
```

## 全链路运行

确认 `quality.json` 为 `safe_to_adapt` 后，再跑完整链路：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && .conda/babelecho-dev/bin/python -m babelecho episode convert --workspace workspace --run-id <run-id> --url "<url>" --local-config workspace/config/local-cosyvoice-on-demand-chunked.yaml --to-stage publish'
```

如果前面已经跑到 `normalize`，可以从 `adapt` 继续：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && .conda/babelecho-dev/bin/python -m babelecho episode convert --workspace workspace --run-id <run-id> --url "<url>" --local-config workspace/config/local-cosyvoice-on-demand-chunked.yaml --from-stage adapt --to-stage publish'
```

如果 DeepSeek chunk 返回 malformed JSON 或临时 429/5xx，当前实现会按 chunk retry 规则重试。若仍失败，先检查 `workspace/runs/<run-id>/run.json`，不要盲目重复全链路。

## Audio-first ASR Smoke

真实 ASR 只在 audio-first 路线验证，不作为 YouTube/RSS/iTunes/article 的 fallback。当前第一步使用 5090D 已安装的本地 `openai-whisper`，不使用云 API。

已确认的 5090D smoke：

- `openai-whisper` 已安装在 `/home/th5090d/miniforge3/envs/babelecho-tts`。
- `torch 2.11.0+cu130` 可用 CUDA，GPU 为 `NVIDIA GeForce RTX 5090 D v2`。
- 公开英文短样本 `jfk.flac` 使用 `tiny.en`、`--device cuda --fp16 True` 可正确产出英文 ASR JSON。
- `audio-asr-jfk-localcli-20260619` 已用 `babelecho audio convert` 跑通真实 `local_cli` ASR：
  - `--to-stage asr` 写出 `asr/raw.json`
  - `provider=openai_whisper`
  - `model=tiny.en`
  - `language=en`
  - `segment_count=2`
  - 从 `--from-stage diarize --to-stage normalize` 继续成功，`quality.recommendation=safe_to_adapt`
- 中文 TTS 样本不要用 `tiny.en --language en` 测质量，否则会产生英文 hallucination；中文样本应使用 multilingual 模型和 `language=zh`。

真实 wrapper 配置示例，放在 ignored runtime config，例如 `workspace/config/local-audio-asr.yaml`：

```yaml
asr:
  provider: local_cli
  command: "/home/th5090d/miniforge3/envs/babelecho-tts/bin/python tools/openai_whisper_asr_wrapper.py"
  model: tiny.en
  language: en
  device: cuda
  # 可选，仅 audio-first 使用；先人工检查 ASR 输出，再加窄短语替换。
  replacements:
    - from: "cloud code"
      to: "Claude Code"
    - from: "Clod coworker"
      to: "Claude coworker"
    - from: "Daniel White Knack"
      to: "Daniel Whitenack"
  extra_args:
    - "--fp16"
    - "true"

diarization:
  provider: none

publish:
  base_url: "https://example.com/babelecho"
```

短音频先放在 ignored 路径，例如 `workspace/sources/audio-asr-smoke.wav`。同步代码后先只跑到 `asr`：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && .conda/babelecho-dev/bin/python -m babelecho audio convert --workspace workspace --run-id audio-asr-smoke-20260619 --audio-file workspace/sources/audio-asr-smoke.wav --local-config workspace/config/local-audio-asr.yaml --to-stage asr'
```

检查 `asr/raw.json`：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && /home/th5090d/miniforge3/envs/babelecho-tts/bin/python - <<'"'"'PY'"'"'
import json
from pathlib import Path
run = Path("workspace/runs/audio-asr-smoke-20260619")
raw = json.loads((run / "asr/raw.json").read_text())
print(json.dumps({
    "provider": raw.get("provider"),
    "model": raw.get("model"),
    "language": raw.get("language"),
    "duration_seconds": raw.get("duration_seconds"),
    "segment_count": len(raw.get("segments") or []),
    "first_segment": (raw.get("segments") or [None])[0],
}, ensure_ascii=False, indent=2))
PY'
```

这一步只验证 ASR runtime 和 `asr.provider=local_cli`；真实 diarization、声纹、长音频 chunking 和 ASR 结果人工校对另走后续阶段。

## Audio-first Diarization Smoke

真实 speaker diarization 只在 audio-first 路线验证，不作为 YouTube/RSS/iTunes/article 的 fallback。

当前代码已支持：

- `diarization.provider=fixture`
- `diarization.provider=none`
- `diarization.provider=local_cli`

已确认的 5090D 状态：

- `/home/th5090d/miniforge3/envs/babelecho-tts` 已有 `torch`、`torchaudio`、`sklearn`。
- 为避免污染 TTS 环境，已从 `babelecho-tts` 克隆独立环境到 `/home/th5090d/miniforge3/envs/babelecho-diarization`。
- `babelecho-diarization` 已安装 `pyannote.audio 4.0.4`；安装后只在该克隆环境中移除了与 NumPy 2.x 不兼容的旧 `onnxruntime-gpu 1.18.0`。
- `babelecho-tts` 保持不动，继续负责 OpenAI Whisper ASR 和 CosyVoice TTS。

建议第一轮真实模型使用 pyannote Community-1，因为它可以本地运行，并且支持 `min_speakers` / `max_speakers`。但它需要先在 Hugging Face 接受模型条款并准备 token，或先按条款把模型下载到本地目录后离线运行。不要把 token 写进 git，也不要放在命令行参数里。

ignored runtime config 示例：

```yaml
asr:
  provider: local_cli
  command: "/home/th5090d/miniforge3/envs/babelecho-tts/bin/python tools/openai_whisper_asr_wrapper.py"
  model: small.en
  language: en
  device: cuda
  extra_args:
    - "--fp16"
    - "true"

diarization:
  provider: local_cli
  command: "/home/th5090d/miniforge3/envs/babelecho-diarization/bin/python tools/pyannote_diarization_wrapper.py"
  model: pyannote/speaker-diarization-community-1
  min_speakers: 2
  max_speakers: 4
  extra_args:
    - "--device"
    - "cuda"

publish:
  base_url: "https://example.com/babelecho"
```

如果使用 Hugging Face 下载模型，token 只放在 5090D ignored 环境文件，例如 `workspace/config/huggingface.env`。文件可写成普通 `HF_TOKEN=...`，运行时用 `set -a` 加载，确保变量导出给 Python 子进程。先确认它被 git 忽略：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && git check-ignore -v workspace/config/huggingface.env'
```

不要在文档、命令行参数、source config 或 commit message 里记录 token 内容。

如果使用本地模型目录，把 `diarization.model` 改成本地目录路径，并确保 wrapper 所在 Python 环境已安装 `pyannote.audio`。

真实 smoke 命令仍先停在 `normalize`：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && set -a && . workspace/config/huggingface.env && set +a && .conda/babelecho-dev/bin/python -m babelecho audio convert --workspace workspace --run-id audio-diarization-smoke-20260619 --audio-file workspace/sources/asr-practicalai-zero-trust-8min.wav --local-config workspace/config/local-audio-diarization-smoke.yaml --to-stage normalize'
```

检查重点：

- `asr/diarization.json` 的 `speaker_count`
- `asr/diarization.json` 的 turn 数和平均 turn 长度
- `transcript/normalized.json` 是否出现 `speaker_1` / `speaker_2` 稳定切换
- `transcript/quality.json` 是否从 `diarization_disabled` 变为真实 diarization 相关 warnings 或 `safe_to_adapt`

这一步不进 DeepSeek、不跑 TTS。

已确认的真实 smoke：

- run-id：`audio-diarization-practicalai-zero-trust-8min-qualitygate-20260619`
- 样本：`workspace/sources/asr-practicalai-zero-trust-8min.wav`，8 分钟，16 kHz mono。
- ASR：OpenAI Whisper `small.en`，`raw_segment_count=123`。
- Diarization：pyannote Community-1，`speaker_count=2`、`turn_count=23`、`avg_turn_ms=20735.7`。
- Normalize：`normalized_segment_count=32`，speakers 为 `speaker_1` / `speaker_2`。
- 旧 Quality：`recommendation=inspect_first`，warning 为 `asr_segment_crosses_speaker_turns`。
- 新 crossing policy：`asr_segment_crosses_speaker_turns` 只作为 advisory warning；quality metrics 写 `cross_speaker_segment_count/ratio`、`ambiguous_speaker_segment_count/ratio`、`min_primary_speaker_overlap_ratio` 和 `avg_primary_speaker_overlap_ratio`，只有模糊 speaker assignment 数量/比例达到阈值时才写 `ambiguous_speaker_assignments` 并 `inspect_first`。
- 该样本已用新代码在 5090D 实际复跑：`recommendation=safe_to_adapt`，`cross_speaker_segment_count=9/123`，`ambiguous_speaker_segment_count=2/123`，`min_primary_speaker_overlap_ratio=0.537`，`avg_primary_speaker_overlap_ratio=0.781`。
- 用该集已有 speaker VTT 粗略对齐，`speaker_1=Daniel`、`speaker_2=Chris`，覆盖片段 overlap accuracy 约 `99.59%`。

当前判断：真实 diarization 分离本身可用；同一 run 已继续跑通 `audio-first -> DeepSeek -> TTS -> publish`，script/manifest 均 32 段，voice roles 为 `female_a/male_a`，最终 MP3 为 `22050 Hz` mono、约 `370.55s`、约 `5.9 MB`。产物已拷回本机 ignored 路径 `workspace/runs/audio-diarization-practicalai-zero-trust-8min-qualitygate-20260619/output/audio.mp3`，用户试听反馈“整体可以接受”。

ASR 模型横评第一轮：

- `small.en`：cached normalize 约 `23.1s`，粗略 WER `0.165`，quality=`safe_to_adapt`；主要专名问题是 `Claude` 多被听成 `cloud`，`Whitenack` 被听成 `White Knack`。
- `medium.en`：首次下载后 cached normalize 约 `28.7s`，粗略 WER `0.178`，quality=`safe_to_adapt`；能抓到 1 次 `Claude Code`，但也多次写成 `Clod`，并把 `Whitenack` 误成 `Witek`。
- 当前结论：`medium.en` 没有在该样本上形成默认升级理由，audio-first 英文 ASR 暂时继续以 `small.en` 作为第一基线。下一步优先看专名纠错策略，或再测 `large-v3` / faster-whisper。

ASR 专名纠错第一版：

- 已支持 audio-first 专属 `asr.replacements`，在 `fixture` / `local_cli` ASR 输出归一化后、写入 `asr/raw.json` 前执行。
- 只做显式短语 `from -> to` 替换，按配置顺序应用；默认不开启，不做自动词典，也不全局默认替换 `cloud`。
- 命中信息写入 `asr/raw.json` 的 `metadata.asr_replacements`，格式为 `{"rules": <规则数>, "replacements": <替换次数>}`。
- 用途：针对已人工确认的样本错词做窄规则，例如 `cloud code -> Claude Code`、`Clod coworker -> Claude coworker`、`Daniel White Knack -> Daniel Whitenack`。如果词本身可能是真实含义，例如普通 `cloud`，不要做宽泛替换。
- 5090D 临时 worktree 已用 Practical AI 8 分钟样本验证：`tests/test_asr.py` 通过，`small.en` ASR 仍输出 123 段；3 条窄规则命中 4 次，示例输出包括 `I am Daniel Whitenack.`、`like Claude Code or Claude coworker, all the cloud things.` 和 `Claude Code or, or something like that...`，同时保留了未确认的普通 `cloud security`。
- 5090D 最新 `main` 已跑真实后流程 smoke：run-id `audio-diarization-practicalai-replacements-normalize-20260620`，`small.en` + pyannote 跑到 `normalize` 仍为 `safe_to_adapt`，ASR 123 段、normalized 32 段、3 条规则命中 4 次；继续 `--from-stage adapt --to-stage adapt` 后 DeepSeek 生成 32 段中文脚本，`babelecho check --checks script` 通过。首段脚本中 speaker_1 为 `Daniel Whitenack`。

Speaker profile contract 第一版：

- `diarize` 阶段会写 `asr/speaker-profiles.json`，当前包含 diarization 统计：speaker id、turn 数、总时长、首尾时间、平均 turn 时长、`sample_count`、`sample_duration_ms`、`profile_kind`、`embedding_status` 和保留的 `embedding_artifact`。
- 该文件不是 voiceprint embedding，也不做真实身份识别。
- audio-first config 支持 `voice_profile.provider=none/fixture`。`none` 保持默认统计 profile；`fixture` 只用于契约测试，可合并 deterministic 元数据，不安装或加载 SpeechBrain、pyannote embedding、NeMo 等真实 embedding 模型。
- fixture 示例：

```yaml
voice_profile:
  provider: fixture
  fixture_path: tests/fixtures/asr/two-speaker-voice-profile.json
```

- publish 阶段如发现该文件，会复制为 episode published artifact `speaker-profiles.json`，并在 `artifact.json.artifacts.speaker_profiles` / `artifact.json.asr.speaker_profiles` 写摘要。
- `artifact.json.asr.speaker_profiles` 不暴露 `embedding_artifact`；真实 embedding 文件如未来存在，只能留在 ignored run-local 路径，不能复制到 `workspace/published/`。
- 5090D 临时 worktree 已用 Practical AI 8 分钟样本跑到 `diarize`：`speaker_count=2`，speaker ids 为 `speaker_1/speaker_2`，所有 profile `embedding_status=not_computed`，`run.json.outputs.speaker_profiles=asr/speaker-profiles.json`。
- 5090D 最新 `main` full publish smoke 已通过：run-id `audio-speaker-profiles-practicalai-publish-20260620`，ASR 123 段、normalized/script/manifest 均 32 段，quality=`safe_to_adapt`，最终 MP3 为 `22050 Hz` mono、约 `361.48s`；published artifact 中 `speaker-profiles.json` 存在，`artifact.json.artifacts.speaker_profiles="speaker-profiles.json"`，`artifact.json.asr.speaker_profiles.embedding_status="not_computed"`。

## Audio-first Voice Profile local_cli Contract

真实声纹 embedding 只在 audio-first 路线验证，不作为 transcript-first fallback，也不进入 voice clone。当前代码已支持：

- `voice_profile.provider=none`
- `voice_profile.provider=fixture`
- `voice_profile.provider=local_cli`

`local_cli` 只定义核心和外部 wrapper 的契约。核心会传入音频、diarization、现有 speaker profiles、输出目录和 summary 路径；wrapper 负责真实模型加载和 embedding 产出。`tools/speaker_embedding_wrapper.py` 当前实现 SpeechBrain ECAPA 路径。

ignored runtime config 示例：

```yaml
asr:
  provider: local_cli
  command: "/home/th5090d/miniforge3/envs/babelecho-tts/bin/python tools/openai_whisper_asr_wrapper.py"
  model: small.en
  language: en
  device: cuda
  extra_args:
    - "--fp16"
    - "true"

diarization:
  provider: local_cli
  command: "/home/th5090d/miniforge3/envs/babelecho-diarization/bin/python tools/pyannote_diarization_wrapper.py"
  model: pyannote/speaker-diarization-community-1
  min_speakers: 2
  max_speakers: 4
  extra_args:
    - "--device"
    - "cuda"

voice_profile:
  provider: local_cli
  command: "/home/th5090d/miniforge3/envs/babelecho-voice-profile/bin/python tools/speaker_embedding_wrapper.py"
  model: speechbrain/spkrec-ecapa-voxceleb
  device: cuda
  min_sample_ms: 1500
  max_samples_per_speaker: 5

publish:
  base_url: "https://example.com/babelecho"
```

注意：

- `babelecho-voice-profile` 是建议的独立 5090D 环境，避免污染 TTS 或 diarization 环境。
- 当前 5090D probe 已选择 SpeechBrain ECAPA：`speechbrain/spkrec-ecapa-voxceleb`。BabelEcho core 仍不能直接依赖 SpeechBrain。
- wrapper 输出的 `asr/voice-profiles/*.json` 只留在 ignored run-local 路径；publish 不复制这些文件。
- `artifact.json.asr.speaker_profiles` 只暴露 `profile_kind` 和 `embedding_status` 等摘要，不暴露 `embedding_artifact` 或向量。

本地契约 smoke 不需要真实模型：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && git pull --ff-only && .conda/babelecho-dev/bin/python -m pytest tests/test_voice_profile.py tests/test_audio_pipeline.py::test_audio_convert_diarize_stage_applies_local_cli_voice_profile -q'
```

已确认的 5090D model probe：

- 独立环境：`/home/th5090d/miniforge3/envs/babelecho-voice-profile`，从 `babelecho-diarization` 克隆后只在该环境安装 `speechbrain 1.1.0` 和 `sentencepiece 0.2.1`。
- 未使用 conda `base`，未改动 `babelecho-tts` 或 `babelecho-diarization`。
- 样本：`workspace/sources/asr-practicalai-zero-trust-8min.wav`。
- Diarization artifact：`workspace/runs/audio-diarization-practicalai-zero-trust-8min-qualitygate-20260619/asr/diarization.json`。
- pyannote embedding：`pyannote/embedding` 被 Hugging Face gated repo 403 阻断，当前 `HF_TOKEN` 未获该模型授权。
- SpeechBrain ECAPA：`speechbrain/spkrec-ecapa-voxceleb` 下载到 ignored `workspace/runs/voice-profile-probe-20260620/speechbrain-spkrec-ecapa-voxceleb` 并成功运行。
- 设备：CUDA，GPU 为 `NVIDIA GeForce RTX 5090 D v2`。
- 抽样：`speaker_1` 5 段、`speaker_2` 4 段，最短样本阈值 `1500 ms`。
- embedding dimension：`192`。
- 运行：首次加载与 probe 总耗时约 `36.9s`，外层 wall time 约 `38.1s`，峰值 RSS 约 `2.4 GB`。
- 分离信号：speaker 内 cosine 均值约 `speaker_1=0.929`、`speaker_2=0.849`，speaker 间 cosine 约 `0.457`。
- 结论：SpeechBrain ECAPA 是当前可用的第一版 speaker embedding backend；因已有可行候选，未继续 NeMo。

`tools/speaker_embedding_wrapper.py` 已按 SpeechBrain ECAPA 实现：它会选取每个 speaker 最长的可用 diarization windows，写 run-local `asr/voice-profiles/<speaker>.json`，并写 `summary.json` 供核心合并到 `asr/speaker-profiles.json`。这仍然不是 voice clone，不把 embedding 用于 TTS。

已确认的真实 wrapper smoke：

- run-id：`audio-voice-profile-speechbrain-smoke-20260620`
- config：ignored `workspace/config/local-audio-voice-profile-speechbrain-smoke.yaml`
- ASR：fixture，用于快速通过 `asr` stage。
- Diarization：fixture 指向已有 Practical AI 8 分钟真实 pyannote artifact。
- Voice profile：`voice_profile.provider=local_cli`，command 指向 `/home/th5090d/miniforge3/envs/babelecho-voice-profile/bin/python tools/speaker_embedding_wrapper.py`，model 为 `speechbrain/spkrec-ecapa-voxceleb`。
- 运行：`babelecho audio convert --to-stage diarize` 成功，外层 wall time 约 `4.52s`，峰值 RSS 约 `2.36 GB`（模型已缓存）。
- `asr/speaker-profiles.json`：`speaker_count=2`，`speaker_1` 和 `speaker_2` 均为 `profile_kind=speaker_embedding`、`embedding_status=computed`。
- `speaker_1`：`sample_count=5`，`sample_duration_ms=236974`，`embedding_artifact=asr/voice-profiles/speaker_1.json`。
- `speaker_2`：`sample_count=4`，`sample_duration_ms=117011`，`embedding_artifact=asr/voice-profiles/speaker_2.json`。
- `asr/voice-profiles/speaker_1.json` 和 `speaker_2.json` 均为 run-local artifact，embedding dimension 为 `192`。
- 同一 run 已用手写中文 fixture script 从 `synthesize` 继续跑到 `publish`，用于验证 publish privacy。
- Published artifact：`artifact.json.asr.speaker_profiles` 只包含 `provider`、`speaker_count`、`profile_kind=speaker_embedding`、`embedding_status=computed`，不包含 `embedding_artifact`。
- Published episode 目录下没有 `asr/voice-profiles/`，未复制私有 embedding artifact；只保留公开 `speaker-profiles.json` 摘要文件。

## 运行后检查

远程完成后，先跑 artifact check：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && .conda/babelecho-dev/bin/python -m babelecho check --workspace workspace --run-id <run-id> --checks script segments output'
```

再看 run、质量、声部和音频元信息：

```bash
ssh my-5090d-host 'cd /home/th5090d/Develop/personal_project/BabelEcho && .conda/babelecho-dev/bin/python - <<'"'"'PY'"'"'
import json
from pathlib import Path
run = Path("workspace/runs/<run-id>")
for rel in ["run.json", "transcript/quality.json", "script/speaker-voices.json", "segments/manifest.json"]:
    path = run / rel
    print(f"\n## {rel}")
    if path.exists():
        data = json.loads(path.read_text())
        if rel == "segments/manifest.json":
            segments = data.get("segments") or []
            print(json.dumps({
                "segment_count": len(segments),
                "voice_roles": sorted({s.get("voice_role") for s in segments if s.get("voice_role")}),
                "first_segment": segments[0] if segments else None,
            }, ensure_ascii=False, indent=2))
        else:
            print(json.dumps(data, ensure_ascii=False, indent=2))
    else:
        print("missing")
PY
ffprobe -v error -show_entries format=duration,size:stream=codec_name,sample_rate,channels -of json workspace/runs/<run-id>/output/audio.mp3'
```

## 拷回本机

通常只拷最终 MP3 和小元数据文件，不拷 100+ 个 wav 分段：

```bash
run='workspace/runs/<run-id>'
remote='/home/th5090d/Develop/personal_project/BabelEcho/workspace/runs/<run-id>'
mkdir -p "$run/output" "$run/transcript" "$run/script" "$run/segments" "$run/publish"
scp "my-5090d-host:$remote/output/audio.mp3" "$run/output/audio.mp3"
scp "my-5090d-host:$remote/run.json" "$run/run.json"
scp "my-5090d-host:$remote/transcript/quality.json" "$run/transcript/quality.json"
scp "my-5090d-host:$remote/script/speaker-voices.json" "$run/script/speaker-voices.json"
scp "my-5090d-host:$remote/segments/manifest.json" "$run/segments/manifest.json"
scp "my-5090d-host:$remote/publish/feed.xml" "$run/publish/feed.xml"
```

本机确认：

```bash
ffprobe -v error -show_entries format=duration,size:stream=codec_name,sample_rate,channels -of json workspace/runs/<run-id>/output/audio.mp3
git status --short --branch
```

`workspace/runs/` 是 ignored runtime 目录，拷回的音频和元数据不要提交。

## 已通过的代表样本

2026-06-19 已在 5090D 上用最新 `main` 跑过标准播客 full-chain 样本，并拷回本机：

| 方向 | URL | run-id | 结果 |
| --- | --- | --- | --- |
| AI / MCP 标准播客 | `https://practicalai.show/358` | `full-practicalai-358-20260619` | 132 段，`safe_to_adapt`，MP3 约 48 分 06 秒，22050 Hz mono |
| 科学叙事 | `https://radiolab.org/podcast/liberation-rna/transcript` | `full-radiolab-rna-20260619` | 100 段，`safe_to_adapt`，MP3 约 23 分 18 秒，22050 Hz mono |
| 设计 / 社会技术 | `https://99percentinvisible.org/episode/666-enshittification/` | `full-99pi-enshittification-20260619` | 148 段，`safe_to_adapt`，MP3 约 32 分 17 秒，22050 Hz mono |
| 多角色压力样本 | `https://99percentinvisible.org/episode/641-99pi-anniversary-special-15-for-15/` | `full-99pi-anniversary-20260619` | 157 段，10 个 speaker，`safe_to_adapt`，MP3 约 47 分 56 秒，约 46 MB，22050 Hz mono |
| MVP-1 单步 RSS 收口 | `https://podnews.net/rss` + `--select-index 1` | `rss-podnews-single-url-full-20260619` | 18 段，`safe_to_adapt`，MP3 约 3 分 54 秒，约 3.6 MB，22050 Hz mono |

这些样本用于回归覆盖：

- Practical AI：AI 内容、MCP 术语、多 speaker、长节目。
- Radiolab：科学叙事、舞台提示、节目尾部版权/转写说明。
- 99% Invisible：设计/社会技术、口语吐槽、法律/产品/品牌术语。
- 99% Invisible Anniversary：10 speaker、多来电者、多制作名单、英文人名和品牌名密集，覆盖多角色分配与长节目压力测试。
- Podnews single URL：覆盖直接 RSS feed URL + `--select-index` 单步入口、碎段合并、无 speaker run 的默认 TTS 路径和短节目 full-chain。

针对上述试听暴露的问题，本轮已实现三层清理：

- `normalize` 会丢弃纯舞台提示段，例如 `[APPLAUSE]`、`[MUSIC]`、`[Radiolab theme music].`。
- `normalize` 会丢弃常见版权尾巴和转写免责声明，例如 `Copyright ... all rights reserved`、`terms of use`、`Transcripts are created ...`。
- DeepSeek adapt prompt 已明确要求清理舞台提示、版权/转写说明，并让 URL、域名、`MP3` / `MP4` 等缩写适合中文 TTS。
- `local_cli` TTS 写入 wrapper `.txt` 前会把 `predictionguard。com`、`99pi.org` 这类域名改成 `predictionguard 点 com`、`99pi 点 org`，并把 `MP三` / `MP3` / `MP四` / `MP4` 改成 `M P 3` / `M P 4`。
- `babelecho check --checks script` 仍会拦截整段英文残留，但允许中文制作名单中夹带英文人名，例如 `Kathy Tu`、`Jayson De Leon`、`Stefan Lawrence`。

仍需人工试听观察：

- 真实节目制作名单、广告口播、赞助说明不默认删除，因为它们可能是节目正文的一部分。
- 带路径的完整 URL、被 LLM 翻成中文数字的域名，以及不同 TTS 模型的缩写读法，后续按真实样本再补更窄规则。
