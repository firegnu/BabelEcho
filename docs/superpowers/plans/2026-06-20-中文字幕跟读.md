# 中文字幕跟随播放 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让只读前端播放中文音频时，下方中文脚本随播放进度高亮当前段、自动平滑滚动、并支持点击段落跳转到该段音频；为此给中文段补上段级时间戳。

**Architecture:** 三条来源路线在 `adapt` 后汇聚，共用 `synthesize/assemble/publish`。在统一后段加段级时间戳：`assemble` 用标准库 `wave` 累加每段 wav 时长回写 `manifest.json`，`publish` 把时间戳按 id merge 进 `transcript.zh.json`（向后兼容：无 manifest 时退回纯拷贝）。前端检测段是否含 `start_ms` 决定是否启用跟读。不改任何来源路线，不改 `synthesize`，不改声音合成、不改音频字节。

**Tech Stack:** Python 3.12 + 标准库 `wave`；pytest；纯静态前端（原生 JS + CSS，无构建、无框架）。

**环境约定（本 worktree）:** 运行 Python/pytest 时用主 checkout 的解释器 + worktree 的 `src`：
`PYTHONPATH="$(pwd)/src" /Users/firegnu/Developer/personal_projs/BabelEcho/.conda/babelecho-dev/bin/python -m pytest ...`
下文用 `PYBIN` 代指 `/Users/firegnu/Developer/personal_projs/BabelEcho/.conda/babelecho-dev/bin/python`。

参考 spec：`docs/superpowers/specs/2026-06-20-中文字幕跟读-design.md`

---

## Task 1: assemble 累加段级时间戳回写 manifest（后端）

**Files:**
- Modify: `src/babelecho/audio.py`
- Test: `tests/test_audio.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_audio.py` 顶部把 import 行
```python
from babelecho.jsonio import write_json
```
改为
```python
from babelecho.jsonio import read_json, write_json
```
然后在文件末尾追加：

```python
def test_assemble_audio_annotates_segment_timings(tmp_path: Path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    run_paths = create_run(Path("workspace"), "timing-run")
    write_silent_wav(run_paths.segments_dir / "0001.wav", duration_seconds=0.5)
    write_silent_wav(run_paths.segments_dir / "0002.wav", duration_seconds=0.3)
    write_json(
        run_paths.segments_dir / "manifest.json",
        {
            "episode_id": "timing-run",
            "segments": [
                {"id": "0001", "audio_path": "segments/0001.wav", "text": "一"},
                {"id": "0002", "audio_path": "segments/0002.wav", "text": "二"},
            ],
        },
    )

    with patch("subprocess.run"):
        assemble_audio(run_paths, {"format": "mp3"})

    manifest = read_json(run_paths.segments_dir / "manifest.json")
    first, second = manifest["segments"]
    assert (first["start_ms"], first["end_ms"], first["duration_ms"]) == (0, 500, 500)
    assert (second["start_ms"], second["end_ms"], second["duration_ms"]) == (500, 800, 300)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONPATH="$(pwd)/src" PYBIN -m pytest tests/test_audio.py::test_assemble_audio_annotates_segment_timings -v`
Expected: FAIL（`KeyError: 'start_ms'`）

- [ ] **Step 3: 实现**

把 `src/babelecho/audio.py` 完整替换为：

```python
import subprocess
import wave
from pathlib import Path

from .jsonio import read_json, write_json
from .paths import RunPaths


def _ffmpeg_concat_line(path: Path) -> str:
    escaped = str(path).replace("'", "'\\''")
    return f"file '{escaped}'"


def _wav_duration_ms(path: Path) -> int:
    with wave.open(str(path), "rb") as handle:
        frames = handle.getnframes()
        rate = handle.getframerate()
    if rate <= 0:
        return 0
    return round(frames / rate * 1000)


def _annotate_segment_timings(run_paths: RunPaths, manifest: dict) -> None:
    cursor_ms = 0
    for segment in manifest["segments"]:
        audio_path = (run_paths.run_dir / segment["audio_path"]).resolve()
        duration_ms = _wav_duration_ms(audio_path)
        segment["start_ms"] = cursor_ms
        segment["end_ms"] = cursor_ms + duration_ms
        segment["duration_ms"] = duration_ms
        cursor_ms += duration_ms
    write_json(run_paths.segments_dir / "manifest.json", manifest)


def assemble_audio(run_paths: RunPaths, audio_config: dict | None = None) -> str:
    manifest = read_json(run_paths.segments_dir / "manifest.json")
    _annotate_segment_timings(run_paths, manifest)
    concat_path = run_paths.output_dir / "concat.txt"
    lines = []
    for segment in manifest["segments"]:
        audio_path = (run_paths.run_dir / segment["audio_path"]).resolve()
        lines.append(_ffmpeg_concat_line(audio_path))
    concat_path.write_text("\n".join(lines) + "\n", encoding="utf-8")

    output = run_paths.output_audio
    command = [
        "ffmpeg",
        "-y",
        "-f",
        "concat",
        "-safe",
        "0",
        "-i",
        str(concat_path),
        "-vn",
        "-codec:a",
        "libmp3lame",
        "-b:a",
        "128k",
        str(output),
    ]
    subprocess.run(command, check=True)
    return str(output)
```

唯一新增：`import wave`、`write_json` import、`_wav_duration_ms`、`_annotate_segment_timings`，并在 `assemble_audio` 读 manifest 后调用 `_annotate_segment_timings`。ffmpeg 命令与返回值不变。

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONPATH="$(pwd)/src" PYBIN -m pytest tests/test_audio.py -v`
Expected: PASS（含原有 `test_assemble_audio_writes_absolute_concat_paths_for_relative_workspace`）

- [ ] **Step 5: 提交**

```bash
git add src/babelecho/audio.py tests/test_audio.py
git commit -m "feat: annotate segment timings in manifest during assemble"
```

---

## Task 2: publish 把时间戳 merge 进 transcript.zh.json（后端，向后兼容）

**Files:**
- Modify: `src/babelecho/publish.py`
- Test: `tests/test_publish.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_publish.py` 末尾追加两个测试（注入 + 无 manifest 降级）：

```python
def test_publish_merges_segment_timings_into_zh_transcript(tmp_path: Path):
    run_paths = create_run(tmp_path, "timing-publish-run")
    run_paths.output_audio.write_bytes(b"fake mp3")
    write_json(
        run_paths.source_json,
        {"run_id": "timing-publish-run", "source_type": "podcast_rss", "title": "T"},
    )
    write_json(
        run_paths.normalized_transcript_json,
        {"episode_id": "timing-publish-run", "segments": [{"id": "1", "text": "Hello."}]},
    )
    write_json(
        run_paths.chinese_script_json,
        {
            "episode_id": "timing-publish-run",
            "segments": [{"id": "1", "speaker": None, "text": "你好。"}],
        },
    )
    write_json(
        run_paths.transcript_quality_json,
        {"recommendation": "safe_to_adapt", "metrics": {}, "warnings": [], "reasons": []},
    )
    write_json(
        run_paths.segments_dir / "manifest.json",
        {
            "episode_id": "timing-publish-run",
            "segments": [
                {
                    "id": "1",
                    "audio_path": "segments/1.wav",
                    "text": "你好。",
                    "start_ms": 0,
                    "end_ms": 640,
                    "duration_ms": 640,
                }
            ],
        },
    )

    publish_episode(run_paths, {"base_url": "https://example.com/babelecho"})

    zh = read_json(
        run_paths.workspace
        / "published"
        / "episodes"
        / "timing-publish-run"
        / "transcript.zh.json"
    )
    assert zh["segments"][0]["start_ms"] == 0
    assert zh["segments"][0]["end_ms"] == 640
    assert zh["segments"][0]["duration_ms"] == 640
    # script/zh.json 不被改动（adapt 纯产物）
    script = read_json(run_paths.chinese_script_json)
    assert "start_ms" not in script["segments"][0]


def test_publish_zh_transcript_has_no_timings_without_manifest(tmp_path: Path):
    run_paths = create_run(tmp_path, "no-manifest-run")
    run_paths.output_audio.write_bytes(b"fake mp3")
    write_json(
        run_paths.source_json,
        {"run_id": "no-manifest-run", "source_type": "podcast_rss", "title": "T"},
    )
    write_json(
        run_paths.normalized_transcript_json,
        {"episode_id": "no-manifest-run", "segments": [{"id": "1", "text": "Hello."}]},
    )
    write_json(
        run_paths.chinese_script_json,
        {
            "episode_id": "no-manifest-run",
            "segments": [{"id": "1", "speaker": None, "text": "你好。"}],
        },
    )
    write_json(
        run_paths.transcript_quality_json,
        {"recommendation": "safe_to_adapt", "metrics": {}, "warnings": [], "reasons": []},
    )

    publish_episode(run_paths, {"base_url": "https://example.com/babelecho"})

    zh = read_json(
        run_paths.workspace
        / "published"
        / "episodes"
        / "no-manifest-run"
        / "transcript.zh.json"
    )
    assert zh["segments"][0]["text"] == "你好。"
    assert "start_ms" not in zh["segments"][0]
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONPATH="$(pwd)/src" PYBIN -m pytest tests/test_publish.py::test_publish_merges_segment_timings_into_zh_transcript -v`
Expected: FAIL（`KeyError: 'start_ms'` —— 当前是纯 copy，无时间戳）

- [ ] **Step 3: 实现**

在 `src/babelecho/publish.py` 中新增一个 helper（放在 `publish_episode` 函数定义之前，例如紧接 `_asr_summary` / `_write_published_index` 这一组模块函数之后）：

```python
def _write_zh_transcript(run_paths: RunPaths, script: dict, target: Path) -> None:
    timings: dict = {}
    manifest_path = run_paths.segments_dir / "manifest.json"
    if manifest_path.exists():
        manifest = read_json(manifest_path)
        for segment in manifest.get("segments", []):
            if "start_ms" in segment:
                timings[segment["id"]] = {
                    "start_ms": segment["start_ms"],
                    "end_ms": segment.get("end_ms"),
                    "duration_ms": segment.get("duration_ms"),
                }
    enriched = dict(script)
    enriched_segments = []
    for segment in script.get("segments", []):
        new_segment = dict(segment)
        timing = timings.get(segment.get("id"))
        if timing is not None:
            new_segment.update(timing)
        enriched_segments.append(new_segment)
    enriched["segments"] = enriched_segments
    write_json(target, enriched)
```

然后在 `publish_episode` 内，把这一行：

```python
    shutil.copy2(run_paths.chinese_script_json, episode_dir / "transcript.zh.json")
```

替换为：

```python
    _write_zh_transcript(run_paths, script, episode_dir / "transcript.zh.json")
```

（`script` 变量已在 `publish_episode` 顶部 `script = read_json(run_paths.chinese_script_json)` 读出；`read_json`/`write_json` 已 import；`RunPaths` 已 import。`transcript.en.json` 的 `shutil.copy2` 不动。）

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONPATH="$(pwd)/src" PYBIN -m pytest tests/test_publish.py -v`
Expected: PASS（含原有三个 publish 测试 —— 它们不写 manifest，走降级分支仍通过）

- [ ] **Step 5: 提交**

```bash
git add src/babelecho/publish.py tests/test_publish.py
git commit -m "feat: merge segment timings into published transcript.zh.json"
```

---

## Task 3: 端到端 fixture 断言 transcript.zh.json 带时间戳（后端回归）

**Files:**
- Modify: `tests/test_end_to_end_fixture.py`（在 `test_run_command_executes_fixture_pipeline` 内追加断言）

- [ ] **Step 1: 追加断言**

在 `tests/test_end_to_end_fixture.py` 的 `test_run_command_executes_fixture_pipeline` 里，找到现有这段：

```python
    assert (
        workspace
        / "runs"
        / "demo"
        / "publish"
        / "episodes"
        / "demo"
        / "transcript.zh.json"
    ).exists()
```

在它之后追加：

```python
    zh_transcript = read_json(
        workspace
        / "runs"
        / "demo"
        / "publish"
        / "episodes"
        / "demo"
        / "transcript.zh.json"
    )
    first_zh = zh_transcript["segments"][0]
    assert first_zh["start_ms"] == 0
    assert first_zh["end_ms"] == 200
    assert first_zh["duration_ms"] == 200
```

（fixture TTS 走 `write_silent_wav` 默认 0.2s @ 16000Hz = 200ms；fixture transcript 为单段。该测试真实经过 assemble 的 ffmpeg 拼接，端到端验证 assemble→publish 时间戳链路。`read_json` 已在该文件 import。）

- [ ] **Step 2: 运行测试确认通过**

Run: `PYTHONPATH="$(pwd)/src" PYBIN -m pytest tests/test_end_to_end_fixture.py::test_run_command_executes_fixture_pipeline -v`
Expected: PASS

- [ ] **Step 3: 后端全量回归**

Run: `PYTHONPATH="$(pwd)/src" PYBIN -m pytest -q`
Expected: PASS，总数 = 原 225 + 3 新增 = 228 passed

- [ ] **Step 4: 提交**

```bash
git add tests/test_end_to_end_fixture.py
git commit -m "test: assert published zh transcript carries segment timings end-to-end"
```

---

## Task 4: 前端渲染挂 data-start/data-end（podcast + 文章朗读）

**Files:**
- Modify: `frontend/js/app.js`

> 前端无自动化测试框架（spec 非目标），验证集中在 Task 7 的 `serve.py` 目视核验。本 task 只做最小、可独立 review 的渲染改动。

- [ ] **Step 1: podcast 脚本段挂时间戳**

在 `frontend/js/app.js` 的 `scriptHtml` 函数里，podcast 分支当前为：

```javascript
    const rows = ep.zh.map((s) => {
      const role = ep.roleOf(s.speaker);
      const spk = s.speaker
        ? `<div class="seg-spk"><span class="seg-badge" style="--role:${BE.roleColor(role)}">${esc(s.speaker)}</span>${role ? `<span class="seg-role">${esc(role)}</span>` : ''}</div>`
        : '';
      return `<div class="seg">${spk}<div class="seg-body"><span class="seg-id">${esc(s.id || '')}</span><p class="seg-text">${esc(s.text)}</p></div></div>`;
    }).join('');
```

替换为（仅 `return` 行加时间戳属性）：

```javascript
    const rows = ep.zh.map((s) => {
      const role = ep.roleOf(s.speaker);
      const spk = s.speaker
        ? `<div class="seg-spk"><span class="seg-badge" style="--role:${BE.roleColor(role)}">${esc(s.speaker)}</span>${role ? `<span class="seg-role">${esc(role)}</span>` : ''}</div>`
        : '';
      const ts = s.start_ms != null ? ` data-start="${s.start_ms}" data-end="${s.end_ms}"` : '';
      return `<div class="seg"${ts}>${spk}<div class="seg-body"><span class="seg-id">${esc(s.id || '')}</span><p class="seg-text">${esc(s.text)}</p></div></div>`;
    }).join('');
```

- [ ] **Step 2: 文章朗读段挂时间戳**

在同一 `scriptHtml` 函数里，文章分支当前为：

```javascript
    if (isArt) {
      const body = ep.zh.map((s) => BE.isHeading(s.text)
        ? `<h3 class="reading-h">${esc(s.text)}</h3>`
        : `<p class="reading-p">${esc(s.text)}</p>`).join('');
      return `<div class="reading">${body}
        <div class="note">正文共 ${segCount} 段 · article_reading 路线为文章正文 + 小标题段，按阅读排版而非主持人转录行。中文无段级时间戳。</div></div>`;
    }
```

替换为：

```javascript
    if (isArt) {
      const body = ep.zh.map((s) => {
        const ts = s.start_ms != null ? ` data-start="${s.start_ms}" data-end="${s.end_ms}"` : '';
        return BE.isHeading(s.text)
          ? `<h3 class="reading-h"${ts}>${esc(s.text)}</h3>`
          : `<p class="reading-p"${ts}>${esc(s.text)}</p>`;
      }).join('');
      return `<div class="reading">${body}
        <div class="note">正文共 ${segCount} 段 · article_reading 路线为文章正文 + 小标题段，按阅读排版而非主持人转录行。播放时高亮跟随，可点击段落跳转。</div></div>`;
    }
```

（同时把该说明里过时的「中文无段级时间戳」改为跟读说明。podcast 分支末尾 `note` 里的「中文脚本无段级时间戳」在 Task 5 一并更新。）

- [ ] **Step 3: 提交**

```bash
git add frontend/js/app.js
git commit -m "feat(frontend): tag zh script/reading segments with playback timestamps"
```

---

## Task 5: 前端跟读控制器（高亮 + 自动滚动 + 手动滚动抑制）+ active 样式

**Files:**
- Modify: `frontend/js/app.js`
- Modify: `frontend/css/app.css`

- [ ] **Step 1: 加跟读状态与控制函数**

在 `frontend/js/app.js` 顶部 IIFE 的状态声明区（现有 `let curEp = null;` 之后）加：

```javascript
  let followSegs = [];          // [{el, start, end}] for the current tab panel
  let followIdx = -1;           // index of currently highlighted segment
  let suppressFollowUntil = 0;  // pause auto-scroll until this ms timestamp (manual scroll)
```

在 `// ---------------- player wiring ----------------` 注释行之前，加两个顶层函数：

```javascript
  // ---------------- follow-along ----------------
  function collectFollowSegs() {
    followSegs = [];
    followIdx = -1;
    const panel = document.getElementById('tabpanel');
    if (!panel) return;
    panel.querySelectorAll('[data-start]').forEach((el) => {
      followSegs.push({ el, start: Number(el.dataset.start), end: Number(el.dataset.end) });
    });
  }

  function followToTime(seconds) {
    if (!followSegs.length) return;
    const ms = seconds * 1000;
    let idx = -1;
    for (let i = 0; i < followSegs.length; i++) {
      if (followSegs[i].start <= ms) idx = i; else break;
    }
    if (idx === followIdx) return;
    if (followIdx >= 0 && followSegs[followIdx]) followSegs[followIdx].el.classList.remove('active');
    followIdx = idx;
    if (idx >= 0) {
      const el = followSegs[idx].el;
      el.classList.add('active');
      if (Date.now() > suppressFollowUntil) el.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }
  }
```

- [ ] **Step 2: 在播放进度更新里调用 followToTime**

在 `wirePlayer` 的 `paint` 函数末尾（`if (elWave) { ... }` 块之后、`paint` 闭合 `}` 之前）加一行：

```javascript
      followToTime(audio.currentTime);
```

- [ ] **Step 3: 渲染后采集段；tab 切换后重采集并恢复高亮**

在 `renderDetail` 末尾，把：

```javascript
    wirePlayer(ep);
    window.scrollTo(0, 0);
```

改为：

```javascript
    wirePlayer(ep);
    collectFollowSegs();
    window.scrollTo(0, 0);
```

在全局 click handler 的 tab 分支里，把：

```javascript
    const tabBtn = e.target.closest('[data-tab]');
    if (tabBtn && curEp) {
      curTab = tabBtn.dataset.tab;
      document.querySelectorAll('.tab').forEach((t) => t.setAttribute('aria-selected', String(t.dataset.tab === curTab)));
      const panel = document.getElementById('tabpanel');
      if (panel) panel.innerHTML = tabPanelHtml(curEp, curTab, curEp.route === 'article_reading');
      return;
    }
```

改为（重渲染 panel 后重采集段并按当前播放时间恢复高亮）：

```javascript
    const tabBtn = e.target.closest('[data-tab]');
    if (tabBtn && curEp) {
      curTab = tabBtn.dataset.tab;
      document.querySelectorAll('.tab').forEach((t) => t.setAttribute('aria-selected', String(t.dataset.tab === curTab)));
      const panel = document.getElementById('tabpanel');
      if (panel) panel.innerHTML = tabPanelHtml(curEp, curTab, curEp.route === 'article_reading');
      collectFollowSegs();
      followToTime(audio.currentTime);
      return;
    }
```

- [ ] **Step 4: 手动滚动暂停自动跟随**

在 IIFE 末尾 `route();` 之前加（用户主动滚动 3 秒内不自动滚，但仍更新高亮）：

```javascript
  const markManualScroll = () => { suppressFollowUntil = Date.now() + 3000; };
  window.addEventListener('wheel', markManualScroll, { passive: true });
  window.addEventListener('touchmove', markManualScroll, { passive: true });
```

- [ ] **Step 5: 更新 podcast note 文案**

在 `scriptHtml` 的 podcast 分支末尾，把：

```javascript
    return `${rows}<div class="note">脚本共 ${segCount} 段 · 含无 speaker 的过场段时不显标签 · 中文脚本无段级时间戳。</div>`;
```

改为：

```javascript
    return `${rows}<div class="note">脚本共 ${segCount} 段 · 含无 speaker 的过场段时不显标签 · 播放时高亮跟随，可点击段落跳转。</div>`;
```

- [ ] **Step 6: active 高亮样式**

在 `frontend/css/app.css` 的 `.note { ... }` 规则（约 146 行）之后追加：

```css
.seg.active { background: color-mix(in srgb, var(--accent-cur, var(--accent)) 8%, transparent); box-shadow: inset 3px 0 var(--accent-cur, var(--accent)); border-radius: 8px; }
.seg.active .seg-text { color: var(--fg); }
.reading-p.active, .reading-h.active { background: color-mix(in srgb, var(--accent-cur, var(--accent)) 10%, transparent); box-shadow: inset 3px 0 var(--accent-cur, var(--accent)); border-radius: 6px; padding-left: 10px; }
[data-start] { cursor: pointer; }
```

（`--accent-cur` 在 `.detail` 元素上按 route 设为 accent / accent-2，`.seg` / `.reading-*` 是其后代，可继承；缺省时回退 `--accent`。）

- [ ] **Step 7: 提交**

```bash
git add frontend/js/app.js frontend/css/app.css
git commit -m "feat(frontend): follow-along highlight, auto-scroll, manual-scroll pause"
```

---

## Task 6: 前端点击段落跳转（双向 seek）

**Files:**
- Modify: `frontend/js/app.js`

- [ ] **Step 1: click handler 加段落 seek**

在全局 click handler 里，找到 rate 分支之后、mobile collapsible 分支之前：

```javascript
    if (e.target.id === 'rate') {
      const steps = [1, 1.25, 1.5, 2, 0.75];
      const next = steps[(steps.indexOf(audio.playbackRate) + 1) % steps.length] || 1;
      audio.playbackRate = next; e.target.textContent = next.toFixed(2).replace(/0$/, '') + '×';
      return;
    }
```

在它之后插入：

```javascript
    const segEl = e.target.closest('[data-start]');
    if (segEl && curEp) {
      audio.currentTime = Number(segEl.dataset.start) / 1000;
      if (audio.paused) audio.play().catch(() => {});
      return;
    }
```

（`[data-start]` 元素只出现在中文脚本/正文 tab 的 panel 内，与 filt/tab/skip/rate/collapsible 控件不重叠，放此处不会误拦截。）

- [ ] **Step 2: 提交**

```bash
git add frontend/js/app.js
git commit -m "feat(frontend): click a script segment to seek audio to it"
```

---

## Task 7: 契约文档同步 + 前端目视核验 + 全量回归

**Files:**
- Modify: `docs/前端Artifact契约与只读界面说明.md`

- [ ] **Step 1: 同步契约文档**

在 `docs/前端Artifact契约与只读界面说明.md` 中描述 `transcript.zh.json` 段结构的位置，补一句：

> 当该集经过 `assemble` 生成音频后，`transcript.zh.json` 每段额外带 `start_ms`/`end_ms`/`duration_ms`（毫秒，基于各段 TTS 音频时长累加）。前端据此启用中文脚本/正文的「随播放高亮 + 自动滚动 + 点击跳转」；旧集若无这些字段则降级为静态展示。`transcript.en.json` 的时间戳为英文原音时间轴，不用于中文跟读。

- [ ] **Step 2: 生成一份带时间戳的本机 fixture 产物供核验**

```bash
PYTHONPATH="$(pwd)/src" PYBIN -m babelecho run \
  --workspace /tmp/be-follow-demo \
  --run-id follow-demo \
  --transcript-file tests/fixtures/sample.vtt \
  --title "Follow Demo" \
  --local-config /dev/stdin <<'YAML'
llm:
  provider: fixture
tts:
  provider: fixture
publish:
  base_url: "https://example.com/babelecho"
YAML
PYBIN -c "import json;d=json.load(open('/tmp/be-follow-demo/published/episodes/follow-demo/transcript.zh.json'));print(d['segments'][0])"
```

Expected: 打印的段含 `start_ms`/`end_ms`/`duration_ms`。

> 注：单段 fixture 足以验证「时间戳存在 + 前端不报错 + 降级判定」。要验证多段高亮切换/滚动手感，用一份真实多段 published 产物（如主 checkout `workspace/published/` 里某集重新 publish 后）更直观；真实语音时长由 5090D 跑，但跟读逻辑本机即可核验。

- [ ] **Step 3: 起前端目视核验**

```bash
PYBIN frontend/serve.py 8137
# 打开 http://127.0.0.1:8137/frontend/
```

核验清单：
- 打开一个**带时间戳**的 episode：播放时当前段高亮、平滑滚动到中部。
- 点击任意段落：音频跳到该段并播放。
- 播放中手动滚动：~3 秒内不被自动拉回；停手后恢复自动跟随。
- 切到「质量 / Metadata」tab 再切回脚本：高亮恢复到当前段。
- 文章朗读集（route=article_reading）的正文同样高亮跟随、可点击。
- 打开一个**旧的、无时间戳**集（如已发布的旧集）：脚本正常静态展示，无报错、无高亮（控制台无异常）。

- [ ] **Step 4: 全量回归**

Run: `PYTHONPATH="$(pwd)/src" PYBIN -m pytest -q`
Expected: PASS（228 passed）

- [ ] **Step 5: 提交**

```bash
git add docs/前端Artifact契约与只读界面说明.md
git commit -m "docs: document zh transcript segment timings and follow-along"
```

---

## 完成后

- 后端改动落在共用后段（`assemble`/`publish`），所有来源路线（YouTube/单URL/RSS/iTunes/episode page/文章/audio-first）自动获得中文段级时间戳。
- 不改 `synthesize`、不改 TTS、不改音频字节；旧集前端自动降级。
- 合并前：按项目收尾规则跑隐私扫描（gitleaks / trufflehog / 手动 grep），更新 `HANDOFF.md` / `resume-prompt.md` 中过期状态，再决定合并/推送。
